name: example
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_mobx: ^2.0.2
  flutter_modular: ^4.0.0+6  # 3.4.1+1 -> with this version works fine, no animation on transition
  mobx: ^2.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^2.1.2
  mobx_codegen: ^2.0.3

flutter:
  uses-material-design: true